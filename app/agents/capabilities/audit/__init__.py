"""能力域 · 审计层：能力调用的审计写入路径（**语义不变**）。

============================  ============================================
模块                           作用
============================  ============================================
``audit``                     能力调用/拒绝的审计记录写入（本地拒绝、租约、
                              授权、派发结果）
============================  ============================================

范围说明（方案 §五）：本阶段**只搬目录**。审计的写入模型、记录格式、查询接口一律不动——
"审计语义统一"是单独立项，不在这里做。把两件事混在一次改动里，出问题时
无法判断是路径问题还是语义问题。

约定：本包刻意不做 re-export（理由见 ``contracts/__init__.py``）。
"""
