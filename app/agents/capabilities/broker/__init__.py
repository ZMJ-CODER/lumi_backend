"""能力域 · 派发层：Provider 选择、租约校验与调用转发。

============================  ============================================
模块                           作用
============================  ============================================
``broker``                    能力调用入口（服务端唯一转发点）：选 Provider、
                              租约/授权校验、幂等、超时、结果收口
``dispatch``                  **旧一代**派发：能力 → MCP 原子工具名 + 参数适配
``resource_dispatch``         **新一代**派发：结构化目标（统一能力 + 资源类型 +
                              Provider Adapter）解析，不再从工具名猜能力
============================  ============================================

两代派发并存（方案 §六）：``dispatch``（``CAPABILITY_TOOL_MAP`` 静态映射）与
``resource_dispatch``（Provider Adapter 解析）在 P2 期间**同时存在**，谁都不是权威实现，
必须先做行为对照再裁决。收敛开关关闭时只有 ``dispatch`` 参与决策。

``broker`` 是本包第二个大文件（1000+ 行），按段标注了四类；它依赖策略层与注册层，
方向是 broker → policy/registry/catalog，**不允许反向**（策略层不得 import broker，
否则 P3 抽纯决策时会绕回运行时）。

约定：本包刻意不做 re-export（理由见 ``contracts/__init__.py``）。
"""
