"""能力域 · 目录层：能力/工具/资源的**声明与派生**（两代实现并存）。

============================  ============================================
模块                           作用                                         四分类
============================  ============================================
``legacy``                    **旧一代**内置能力目录：能力常量、输入/输出    配置加载
                              JSON schema、``_BUILTIN_DESCRIPTORS``、
                              ``CapabilityCatalog``
``resource``                  **新一代**统一资源能力目录：统一能力名、资源    配置加载
                              类型、Provider 声明、工具绑定、可见性三态
``tool_registry``             工具注册表条目派生（静态表 ∪ 目录 ∪ 运行时      纯决策 + 运行时适配
                              能力 ∪ 影子注册表）+ 档位/审批 + 影子对拍
============================  ============================================

**两代并存是刻意的**（方案 §六）：``legacy`` 与 ``resource`` 谁都不是权威实现，
必须先做行为对照（能力条目 / Provider 映射 / 预检结果 / Broker 路由 / 测试覆盖）
再裁决，因此 P2 不合并、不删任何一个。

``tool_registry`` 是本包最大的文件（1600+ 行），方案禁止"整文件搬迁"：它按段标注了
四类（纯决策 / 运行时适配 / 配置加载 / 业务适配），逐段拆分后再决定去向。

约定：本包刻意不做 re-export（理由见 ``contracts/__init__.py``）。
"""
