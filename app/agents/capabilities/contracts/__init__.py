"""能力域 · 契约层：描述协议、授权事实（**纯数据**，无运行时依赖）。

本子包只放"被别的层引用的事实形状"：

============================  ================================================
模块                           作用
============================  ================================================
``context``                   ``AgentExecutionContext``：服务端授权事实
                              （工作区/项目/审批指纹），由 API 层构造后向下传递
============================  ================================================

约定：本包**刻意不做 re-export**。``app/agents/capabilities/__init__.py`` 已经承担了
"包级稳定入口"的职责，子包再加一层转发只会让 import 链更难读，而且会把运行时适配模块
拖进纯决策模块的导入路径里（P3 抽包时要按依赖方向判断，多一层转发就多一层噪音）。
"""
