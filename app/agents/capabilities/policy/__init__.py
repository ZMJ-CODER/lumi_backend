"""能力域 · 策略层：门禁裁决与策略包（**纯决策**，不碰 Redis/DB/FastAPI）。

============================  ============================================
模块                           作用
============================  ============================================
``gate``                      能力门禁：调用前的准入裁决（结合目录与解析结果）
``policy_guard``              插件策略守卫：审批令牌签发/校验、能力指纹
``policy_packs``              策略包（成本优先 / 企业审计 / 高精度 / 手工提交）
``approvals``                 审批裁决与令牌生命周期
``routing``                   能力路由模式（off / shadow / read_only / active）
``resource_window``           统一预检的**工具窗口**：意图 → 能力 → 工具、
                              同资源读取保护（读前置）
``resource_workflow``         Workflow 的能力声明解析与依赖推导
============================  ============================================

这一层是 P3 抽 ``lumi_capability`` 的主要候选区（方案 §P3：能力描述协议、能力状态机、
纯门禁裁决、能力匹配与选择算法、审计记录结构、错误码）。**因此这里禁止出现**
Redis / DB / settings / FastAPI 的运行时调用——一旦混进来，P3 就得把它留在 app 里，
而"混进来"通常只发生一次、之后就再也拆不干净。

约定：本包刻意不做 re-export（理由见 ``contracts/__init__.py``）。
"""
