"""能力域 · 投影层：给前端/管理端的只读视图与快照（**不参与决策**）。

============================  ============================================
模块                           作用
============================  ============================================
``views``                     管理端/前端的只读投影（注册表、目录、租约视图）
``snapshots``                 能力调用快照（审计与排障用的不可变记录）
``resource_surface``          模型可见工具面的收敛投影：Read/Write/Edit/Move/
                              Delete/Run/Search、隐藏清单、差异行
============================  ============================================

边界：本层**只读**——它可以读目录/注册表/派发解析，但任何决策都不许依赖它。
``resource_surface`` 同时被模型注入路径（chat_graph / react_runner / Workflow）复用，
所以它必须保持"开关关闭时恒等"的语义（见该模块 docstring）。

约定：本包刻意不做 re-export（理由见 ``contracts/__init__.py``）。
"""
