"""Typed, declarative policy primitives for orchestration decisions.

Keep package initialization empty: routing patterns and feature snapshots are
also used by the legacy router, so eager imports here would create a cycle.
"""
