"""Knowledge 域 · 嵌入层：文本/代码 → 向量。

============================  ================================================
模块                          作用
============================  ================================================
``embeddings``                bge 模型嵌入（``embed_query`` / ``embed_texts``）
``sparse_embeddings``         稀疏向量（词法召回那一路）
``code_embedding``            本地代码向量（代码索引用）
============================  ================================================

依赖方向：本层被解析层（``classifier``）与检索层（``knowledge``）复用；
它自己只依赖模型/配置，不反向依赖它们。
"""
