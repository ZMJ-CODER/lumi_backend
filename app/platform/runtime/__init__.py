"""``app/platform.runtime``：运行时设施（与业务无关的"怎么跑"）。

============================  ================================================
模块                          作用
============================  ================================================
``deadline``                  截止时间与超时阶梯（把剩余预算算成具体超时）
``executors``                 线程/进程执行器（计算密集任务不阻塞事件循环）
``feature_flags``             灰度开关（默认关闭时行为逐字不变）
``resilience``                重试/熔断/退避
``read_view_cache``           只读视图缓存（带失效条件）
============================  ================================================

纪律：这一层**不认识任何业务概念**。出现 job / memory / workspace 之类的词，
说明它其实是领域服务，应该回 `app/<domain>/`（门禁规则 6 也盯着 `app/core` 的同类问题）。
"""
