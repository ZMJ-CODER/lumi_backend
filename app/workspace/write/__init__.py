"""Workspace 域 · 写入一侧。

============================  ================================================
模块                          作用
============================  ================================================
``operations``                暂存 / 提交 / 删除等写操作（``WorkspaceClient`` 与
                              ``NavigatorWorkspaceClient`` 两种执行端）
``revision``                  修订与比对（文本/目录修订值、换行策略、单次替换）
``trash``                     回收站（记录、配额、恢复、过期）
============================  ================================================

纪律：**这一侧的每个入口都要过写闸**（租约 / 审批 / 版本校验 / 回收站），
不允许出现"直接落盘"的旁路。新增写能力时，先看 ``operations`` 里既有的
"暂存 → 校验 → 提交"三段式，别新写一条路径。
"""
