# mcp / MCP 生态工具

分类说明：Model Context Protocol（MCP）生态相关工具，例如：

- 连接/调用外部 MCP 服务器（filesystem / github / slack 等官方或第三方 MCP server）
- MCP 工具发现与动态注册（把外部 MCP 工具桥接为本地 Skill）

当前为空，等待实现。新增技能时在本目录放一个 `.py` 文件（下划线开头会被跳过）。
